//test error

int main() {
    int a = 0;
    int i = 0;

    a = a + (1 << i); // In L1, the bit-shift operator is not present
    i = i + 1;
    a = a + (1 << i);
    i = i + 1;
    a = a + (1 << i);
    i = i + 1;
    a = a + (1 << i);
    i = i + 1;
    a = a + (1 << i);
    i = i + 1;
    a = a + (1 << i);
    i = i + 1;
    a = a + (1 << i);
    i = i + 1;
    a = a + (1 << i);

    return a;    
}
