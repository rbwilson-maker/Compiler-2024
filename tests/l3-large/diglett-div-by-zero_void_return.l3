//test div-by-zero

void foo(int i) {
  for (int j; i != 0; i--)
    print_int(i);

  if (1 < 2) {
    for (int j=1; j == 0; j++)
      return;
    int k = i / i;
  }
  else {
    fpt f = itof(i);
  }
}

int main() {
  foo(14);
  return 0;
}

