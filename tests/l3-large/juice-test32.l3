//test return 12

int foo(int x) {
  x++;
  x++;
  return x;
}

int main() {
  int x = 2;
  int y = 3;
  int z = 5;
  while (z > 0) {
    assert(z > 0);
    x = foo(x);
    y++;
    z--;
  }
  return x;
}