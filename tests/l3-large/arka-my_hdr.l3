//test typecheck

int main() {
    int sum = 0;
    for(int i = 0; i < 1000; i++) {
        sum += weird_succ(i);
    }

    for(int i = 1000; i >= 100; i--) {
        sum += weird_pred(500 - i);
    }

    return sum;
}