//test error

int odd (int x) {
  if ((x % 2) == 0)
    return even(x);
  else
    return even(x - 1);
}

int even (int x) {
  if (x == 0) {
    return x;
  }
  if((x % 2) == 1)
    return odd(x);
  else
    return odd(x - 1);
}

int main () {
  int odd(7);
}