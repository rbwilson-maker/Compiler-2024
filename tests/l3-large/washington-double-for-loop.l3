//test return 50

int sum(int i);

int main() {
  return sum(5);
}

int sum(int x) {
  int ans = 0;
  for (int i = 0; i < x; i++) {
    for (int j = 0; j < x; j++) {
      ans += 4*i - 3*j;
    }
  }
  return ans;
}
