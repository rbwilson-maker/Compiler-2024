//test return 3
//testing external function calls to floating point operations

int main() {

  fpt x = 2;
  int y = 0;
  for (int z = 3; z < 5; z++) {
	y++;
	x = fadd(x, itof(y));
  }

  return ftoi(x);

}