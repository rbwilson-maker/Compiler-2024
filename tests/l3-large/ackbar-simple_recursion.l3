//test return 42
//A test of basic recursion

int countDown(int x){
    if( x > 0){
        return 1 + countDown(x-1);
    }
    else {
        return 0;
    }
}

int main(){
    return countDown(42);
}
