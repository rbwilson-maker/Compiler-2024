//test return 22

typedef int a;
typedef bool b;

b sumZero(a x, a y) {
    return x + y == 0;
}

int main () {
    if (sumZero(3, -1)) {
        return -1;
    } else if (sumZero(4, -2)) {
        return -2;
    } else {
        return 22;
    }
}