//test return 2450

bool is_even(int n) {
    if (n % 2 == 0) {
        return true;
    }
    return false;
}

int main() {
    int sum_of_evens = 0;
    for (int i = 0; i < 100; i++) {
        if (is_even(i)) sum_of_evens += i;
    }

    return sum_of_evens;
}