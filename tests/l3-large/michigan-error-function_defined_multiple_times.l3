//test error

int fib(int x) {
  if (x == 0 or x == 1) {
    return 1;
  }
  else {
    return fib(x-1) + fib(x-2);
  }
}

int fib(int x) {
  // Magical incorrect closed form
  return x*10;
}

int main() {
  return fib(5);
}