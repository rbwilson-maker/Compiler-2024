//test return 1
//multple decleration is allowed
bool isPrime(int p);

bool isPrime(int p){
  for(int i = 2; i < p; i++){
    if(p % i == 0) return false;
  }
  return true;
}

bool isPrime(int p);

int main();

int main(){
  if(isPrime(5))
    if(!isPrime(4))
      return 1;
  return 0;
}

bool isPrime(int p);