//test div-by-zero

int numerator(int a){
    return a * 2 + a;
}

int denominator(int b, int c){
    return b + c - b - c;
}

int main(){
    int x = 4;
    int y = 2;
    int z = 1;

    return numerator(x) / denominator(y,z);
}