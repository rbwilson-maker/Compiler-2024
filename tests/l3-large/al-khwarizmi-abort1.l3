//test abort

int triangleNumber(int n) {
    int res = 0;
    for (int i = 1; i <= n; i++) {
        res += i;
    }
    return res;
}

int main() {
    int t = triangleNumber(10);
    assert(t > 100);
    return 0;
}