//test abort

int sqrt(int x) {
  assert(x >= 0);
  if (x == 0) return 0;
  if (x == 1) return 1;
  int i = 0;
  while (x > i * i) {
    i++;
  }
  return i - 1;
}

int main() {
  return sqrt(-27);
}
