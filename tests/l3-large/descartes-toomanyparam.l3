//test error

//program defines a helper function and utilizes it with too many parameters

bool is_zero(int x) {
    return (x == 0);
}

int main() {
    if (is_zero(0,1)) {
        return 0;
    }
    else {
        return 1;
    }
}