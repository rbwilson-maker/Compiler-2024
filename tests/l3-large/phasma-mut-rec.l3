//test return 0

int sub(int x, int y);

int add(int x, int y) {
  if (x == 2) {
    return -6;
  }
  return sub(x,y/5);
}

int sub(int x, int y) {
  if (y == 5) {
    return 0;
  }
  return add(x/2,y);
}

int main() {
  return add(3460, 125);
}

