//test return 76
//test function usage

int myadd(int x, int y, int z) {
  return x + y + z;
}

int main()
{
  int a = 10;
  int b = 20;
  return myadd(myadd(a, myadd(a, b, 10), 10), myadd(1, 2, 3), a);
}