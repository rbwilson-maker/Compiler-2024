//test error
// Function overloading is not supported

int add(int x, int y) {
  return x - y;
}

int add(bool x, int y) {
  if (x == true) {
    return 0;
  } else {
    return 1;
  }
}


void main() {
  int w = 123;
  int x = 456;
  bool y = true;
  int z = 0;
  z = add(w, x) + add(y, z);
  return;
}