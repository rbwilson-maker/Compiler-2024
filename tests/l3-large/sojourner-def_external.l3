//test error
//defines external function

void get_x(int x) {
  x++;
  return;
}

bool abs(int i) {
    if (i > 0) {
        return true;
    }
    return false;
}

int main() {
  bool res = abs(1);

  if (res) {
    return 1;
  }

  return 0;
}