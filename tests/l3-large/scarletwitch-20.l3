//test return 0
/* Simple function call to sum up numbers */

typedef int t;
int sum(t i, t j) {
  t sum = 0;
  for (; i <= j; i+=1)
    sum += i;
  return sum;
}

int main () {
  t n;
  t x;
  t y;
  n = 100;
  x = sum(1,100);
  y = n*(n+1)/2;
  return x-y;
}
