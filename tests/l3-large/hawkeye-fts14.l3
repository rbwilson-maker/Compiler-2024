//test error

int add10 (int x, int y) {
    int sum = 0;
    for (int i = 0; i < 10; i++)
        sum += x + y;
    return sum;
}

int main () {
    int u = 3;
    int v = 5;
    return add10(u, v, 3);
}