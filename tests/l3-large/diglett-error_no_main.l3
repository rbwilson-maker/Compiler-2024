//test error

bool itob(int i) {
  if (i == 14)
    return false;
  if (i == 13)
    return true;
  assert(false);

  return false;
}

int btoi(bool b);
int btoi(bool a);
int btoi(bool c);
int btoi(bool v);
int btoi(bool d);

int btoi(bool b) {
  return b ? 13 : 14;
}

