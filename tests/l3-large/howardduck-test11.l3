//test return 256

// calculate base^exp
int pow(int base, int exp) {
    int ret = base;
    for(int i = 0; i < exp; i++) {
        ret *= base;
    }
    return ret;
}

int main() {
    int x = pow(2, 2);
    int y = pow(2, 1);
    return x * x * y;
}