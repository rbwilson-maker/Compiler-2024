//test div-by-zero

int division_helper(int x) {
    if (x % 2 == 0) {
        return x / 0;
    }
    else {
        return x / 2;
    }
}

int main()
{
    int res = division_helper(10000);
    return res;
}