//test return 91125

int func(int x, int y, int z) {
  return x*y*z;
}

int main() {
  int w = 0;
  for (int x = 0; x < 10; x++) {
    for (int y = 0; y < 10; y++) {
      for (int z = 0; z < 10; z++) {
        w += func(x, y, z);
      }
    }
  }

  return w;
}
