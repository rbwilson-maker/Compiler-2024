//test abort

typedef int z;

int main() {
  z rand = 1;
  int y = 0;
  for (int i = 0; i < 4*5*6*7*8*9*10; i+= rand) {
    z timestwo = rand * 2;
    y = y != y ? y / 0 : rand;
  }
  assert(true == false);
  return 0;
}