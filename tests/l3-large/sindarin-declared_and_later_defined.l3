//test return 8
// functions declared but not defined

int undefined_helper(bool b, int i);

int helper(bool b, int i) {
  return i + 1;
}

int main() {
  int result = helper(false, 2);
  return result + undefined_helper(true, 3);
}

int undefined_helper(bool b, int i) {
  return i + 2;
}