//test error

typedef int foo;

foo foo(int x) {
  while (x == 0 || x == 0 || x == 0 || x == 0 || x == 0 || x == 0 || x == 0 || x == 0 || x == (x == 0 ? 1 : 0) || x == 0) {
    return x;
  }
}

foo main() {
  return foo(5);
}
