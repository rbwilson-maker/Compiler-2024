//test return 69420

bool isPerfect(int a) {
	int sum = 0;
	for(int i = 1; i < a; i++) {
		if(a % i == 0) sum += i;
	}
	return sum == a;
}

int main() {
	assert(isPerfect(6));
	assert(!isPerfect(11));

	return 69420;
}
