//test return 22

int add(int x, int y);

typedef int number;

int add(number x, int y);

number add(number a, number b);

int add(int y, int x);

number add(int f, number s) {
  return f + s;
}

// tests multiple declarations of the same function
int main() {
    return add(10, 12);
}
