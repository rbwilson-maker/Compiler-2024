//test error

// nearly identical to algerba except for one key difference
bool nightmareWorld () {
    bool x = false;
    x = x && true;
    x = x && true;
    x = x && true;
    x = x && true;
    x = x && true;
    x = x || true;
    x = x && false;
    x = x && true;
    x = x && true;
    x = x && true;
    x = x && true;
    x = x && true;

    return x;
}

int main () {
    assert (nightmareWorld);
    return 0;
}