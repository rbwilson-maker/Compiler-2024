//test div-by-zero

int bitShifter(int num, int shift){
  return num << shift;
}

int main(){
  int x = 0;
  for (int i = 0; i < 50; i++){
    assert(i >= 0);
    x++;
  }

  return bitShifter(1, x);
}