//test typecheck

int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);
int complexcomputation(int b);

int main() {
  return complexcomputation(10);
}
