//test compile

fpt big(int a, int b, int c, int d, int e, int f, int g) {
	int res = 0;
	if (a > b) {
		res += c;
	} else{
		res = d;
		if (res < e) {
			res = fadd(c,e);
		}
		if (res == f) {
			return fmul(g,g);
		}

	  return 1;
	}
	return res;
}

int main() {
	int a = 3;
	int b = 4;
	int c;
	c = a-b;
	return big(a,b,c,c,b,a,a+b);
}




