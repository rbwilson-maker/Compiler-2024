//test return 5
// calcualte the 5th fibonacci number recursively

int fib(int i);

int main() {
  return fib(5);
}

int fib(int i) {
  if (i == 0) {
    return 0;
  } else if (i == 1) {
    return 1;
  } else {
    return fib(i-1) + fib(i-2);
  }
}
