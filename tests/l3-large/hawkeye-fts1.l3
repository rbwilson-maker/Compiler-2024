//test return 1


int foo (int n) {
    if (n == 10)
        return 1;

    if (n == 3)
        return foo(10);

    if ( n <= 5 && n >= -1)
        return foo(3);

    return 0;
}

int main () {
    return foo(5);
}