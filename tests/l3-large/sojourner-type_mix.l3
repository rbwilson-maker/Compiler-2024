//test error
//defines external function

void get_x(int x) {
  x++;
  return;
}

bool greater(int i) {
    if (i > 0) {
        return true;
    }
    return false;
}

int main() {
  bool res = greater(1);

  if (res + 3 > 4) {
    return 1;
  }
  
  return 0;
}