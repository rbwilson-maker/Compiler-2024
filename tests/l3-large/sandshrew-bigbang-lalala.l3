//test return -315

int foo(int x, int y) {
  int z = 0;

  if (true) {
    for ( ; x < y * 1 * (431 / 431); x += 1) {
      z += (3 + y) * ((1 + x) + (2 + x)) + ((3 + x) + ((x + 4) + (5 + x))) +
           (y + 3) * ((2 + x) + (x + 1)) + ((x + 3) + ((5 + x + x + 4)));
      z -= 7 * x + 7 * y + 7 * (x+y) + 7 * (y+x) + 7 * (2 * y) + 7 * x;
    } 
  }
  return z;
  
  for (x = 0; x < y; x++) {
    x *= 2 * 5 * 2 * 7;
  } 
}

int main() {
  return foo(4,9) + foo(1,0xDEADBEEF) + foo(9,4) + foo(0, 0xBEEFDEAD);
}
