//test typecheck

int main() {
    clock_t start = clock();
    for(int i = 0; i < 10000; i++) {
        assert(true);
        assert(true == true);
    }
    clock_t end = clock();
    return end - start;
}