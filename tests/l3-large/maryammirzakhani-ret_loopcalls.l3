//test return 1

bool is_even(int x) {
    return (x % 2 == 0);
}

int main() {
    int out = 1;
    for (int i = 2; is_even(i/1); i += 2) {
        if (i > 100) i++;
        out = 1;
    }
    return out;
}