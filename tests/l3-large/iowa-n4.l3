//test abort

bool isPrime(int n) {
    int d = 2;
    while (d < n) {
        if (n % d == 0)
            return false;
    }

    return true;
}

int main() {
	assert(false);
    int i = 0;

    if (!isPrime(101))
        return 0;

    if (!isPrime(17))
        return 0;

    return 0;
}
