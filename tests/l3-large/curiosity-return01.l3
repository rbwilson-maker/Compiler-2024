//test return 1

bool isPrime(int p){
  for(int i = 2; i < p; i++){
    if(p % i == 0) return false;
  }
  return true;
}

int main(){
  if(isPrime(5))
    if(!isPrime(4))
      return 1;
  return 0;
}