//test return 4

int foo(int x, int y) {
  int z;
  z = x + y;
  return z;
}

int bar(int x, int y) {
    int z;
    z = x - y;
    return z;
}

int main() {
    int z = 5;
    int x = 6;
    int y = 7;
    return foo(z + 1, bar(x + 2, y + 3));
}
