//test return 24
// nested function call

int add(int a, int b) {
    return a + b;
}

int mul(int a, int b) {
    return a * b;
}

int div(int a, int b) {
    return a / b;
}

int f(int a, int b) {
    return mul(add(a, b), div(a, b));
}

int main() {
    return f(6, 2);
}