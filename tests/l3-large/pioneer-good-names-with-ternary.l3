//test div-by-zero
//function names and variable names can be the same

bool is_this_allowed(bool is_this_allowed)
{
  return !is_this_allowed;
}

int main()
{
  int main = 6;
  bool x = !is_this_allowed(false) || is_this_allowed(is_this_allowed(false));
  return x ? 24 : (main / 0);
}
