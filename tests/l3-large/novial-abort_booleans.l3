//test abort

bool ret_true() {
  return true;
}

bool assert_false()  {
  assert (false);
  return true;
}

int main() {
  int x = 120;
  if (x > 0) {
    assert_false();
  } else {
    ret_true();
  }
  return 0;
}
