//test typecheck
//test infloop

int addOne(int x) {
  x++;
  return x;
}

int minusOne(int x) {
  x--;
  return x;
}

int main() {
  int x = 1;
  while(x > 0) {
    x = addOne(x);
    x = minusOne(x);
  }
  return x;
}
