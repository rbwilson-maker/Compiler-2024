//test typecheck

// function approximates the log of a given parameter n 
// using the nth harmonic number

fpt approxLog(int n) {
	fpt sum = 0;
	int i = 1;
	while (i <= n) {
	  sum += 1/i;
	  i++;
	}
	return sum;
}

int main() {
	int n = 10;
	fpt result = approxLog(10);
	return 0;
}