//test return 0

// tests ensure large argument functions work

int f(int a, int b, int c, int d, int e, int f, int g, int h, int i) {
	return a + b + c + d + e + f + g + h + i;
}

int main() {
	int x = 0;
	int y = 0;

	f(1, 2, 3, 4, 5, 6, 7, 8, 9);

	assert(x == 0 && y == 0);

	return 0;
}