//test div-by-zero
//Divide by zero

int main() {
    int x = 10;
    int y = x + 10;
    int z;
    int t = x > y ? x : y;
    while (t == t) {
        y -= 1;
        x = x >> y + y;
        y = y / x;
    }
    return y;
}
