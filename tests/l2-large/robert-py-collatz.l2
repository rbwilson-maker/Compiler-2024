//test return 287

/* This test autogenerated by tests.py/framework for filename collatz */

int main() {
  // Iteration counter for collatz loops
  int x0 = 0;
  int x1;
  // Find number of iterations for collatz(1005). It should be 67
  x1 = 1005;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  // Find number of iterations for collatz(3). It should be 7
  x1 = 3;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  // Find number of iterations for collatz(1). It should be 0
  x1 = 1;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  // Find number of iterations for collatz(10). It should be 6
  x1 = 10;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  // Find number of iterations for collatz(4000). It should be 113
  x1 = 4000;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  // Find number of iterations for collatz(25). It should be 23
  x1 = 25;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  // Find number of iterations for collatz(433). It should be 27
  x1 = 433;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  // Find number of iterations for collatz(1337). It should be 44
  x1 = 1337;
  while ((x1!=1)) {
    if (((x1%2)==0)) {
      x1 = (x1/2);
    }
    else {
      x1 = ((x1*3)+1);
    }
    x0++;
  }
  return x0;
}

/* This is the end of the program */

