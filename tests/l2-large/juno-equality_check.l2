//test return 2

// Tests that equality is overloaded, and works for both ints and bools

int main () {
    int a = 1;
    int b = 1;
    bool c = true;
    bool d = true;
    if (a == b && c == d) {
        a = 2;
    }

    return a;
}