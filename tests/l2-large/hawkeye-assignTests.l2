//test return 5

int main() {
    int a = 2;
    int b = 2;
    int c = 2;
    int d = 2;
    int e = 2;

    b += 5;
    b -= 2;
    return b;   // should be 5

    b *= c;
    return b;   // should be 10

    b -= 5;
    return b;   // should be 5

    b /= 5;     // should be 1
    return b;

    b |= 2;
    return b;   // should be 3

    b %= 2;
    return b;   // should be 1

    b &= 0;
    return b;   // should be 0

    b ^= 3;
    return b;   // should be 3
}
