//test error

int main() {
    // parentheses do not match
    int x = (((1 + 2) - 3 * 4 + 5;
    int z = 0;
    while (x < 10) {
        int y = 0;
        while (y < 10) {
            y++;
            x = 7;
            x-=y;
            // redefine x
            while (int x = 3; x < 10; x++) {
                x += 1;
            }
        }
        x++;
    }
    return x;
}