//test return 30

int main() {
    int x = 0;
    while(x != 5) {
        x++;
    }

    for(int i = 0; i < 10; i++) {
        x += i >> 1; // more precedence!
    }

    int i = 5; // does the for loop get its own symboltable?
    return x + i;
}
