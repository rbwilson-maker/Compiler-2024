//test return 25
int main() {
    int prime_cnt = 0;
    for (int i = 2; i <= 100; i++) {
        bool is_prime = true;

        for (int j = 2; j < i; j++) {
            if (j == 0) {} else
            if (i % j == 0) { is_prime = false; }
            if (prime_cnt < 100) {} else {prime_cnt = 1000;}
            if (j == 1) is_prime = true; else {}
        }
        if (is_prime)prime_cnt += 1; else if (true == false) prime_cnt = -1;
    }
    return prime_cnt;
}
