//test return 14

int main() {
    int a = 12;
    int b = 13;
    int c = 14;
    int d = 15;

    int ret = (a > b) ? (
        (c > d) ? c : d
    ) : (
        (c < d) ? c : d
    );

    return ret;
}
