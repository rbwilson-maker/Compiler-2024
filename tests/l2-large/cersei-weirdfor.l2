//test return 18

int main() {
  int result = 0;
  for(int i = 0; i < 10; i++) {
    // This is a bit of a weird loop, but it should still work 
    result += i;
    i = i + 2;
  }
  return result;
}
