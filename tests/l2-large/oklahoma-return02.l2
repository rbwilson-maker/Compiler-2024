//test return 15100
//Operator associativity and precedence

int main () {

  int a = 1000 / 5 / 2;
  int b = 1000 - 500 - 200;
  int c = a + a * b / 2;
  int d = a + a * (a + a) - b % (a + 1);

  if (a < b != c > d) {
    return c;
  } else {
    return d;
  }
}

  