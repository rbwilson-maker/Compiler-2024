//test div-by-zero

int main() {
    int a = 5 >> 2 / (3 - 3);
    if(a > 3) {
        a++;
    }
    int b = a % 5 + 2;
    while(a > 0) {
        a /= b;
    }
    bool c = true;
    if(c || false) {
        return a - b;
    }
    return a + b;
}