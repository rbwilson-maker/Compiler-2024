//test typecheck
//test infloop

int main() {
    int left = 1;
    int right = 10;

    while (left < right) {
        int mid = (left + right) / 2;
        if (mid < right) {
            left = mid;
        } else {
            right = mid;
        }
    }

    return right;
}