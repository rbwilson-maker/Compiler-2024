//test typecheck
//test infloop
//Test associativity of branches.

int main() {
    int y = -1;
    int x = 0;
    if (y > 3) 
        if (y < 1)
            x -= 2;
        else
            x += 2;
    while (x >= 0) y /= y;
    return y;
}
