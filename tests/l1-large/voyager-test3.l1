//test error

// this file tests uninitialized declaration 

int main() { 
    int a = 3;
    int b = 4;
    int c = a + b * a + b;
    return c;

    int x;
    int y;
    // x and y are declared after return but not initialized +
    // there is a valid control flow path =>
    // so variables must be initialized
    int z = x + y; 
}