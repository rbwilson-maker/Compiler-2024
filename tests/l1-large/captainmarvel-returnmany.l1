//test return 14
int main()
{
    int x1;
    x1 = 1;
    int x2;
    x2 = 2;
    int x3;
    x3 = 3;
    int x4;
    x4 = 4;
    int y;
    y = (x3 + x4) % (x1 + x2);
    int z;
    z = (y + 3) - (x4 + x2);
    int a;
    a = (y + 5) - (x3 - x2) * (x4 - y) * (x1 - z) % (y - z);
    int b;
    b = x1 + x3 - a % z + y * x1 - x3 + x2 + z;
    return a + x1 - y + b % x3 - z * 3;
}