//test return 0
int main () {
	int a = 1;
	int b = 0;
	int c = 0;
	int d = 3;
	int e = 3;
	int f = 4;
	int g = 4;
	int h = 6;
	int i = 7;
	int j = 8;
	int k = 0;
	int l = 0;
	int m = 0;

	a = 1 * - ( 2 / -1 ) * 3 + 4 - 5 / 2;
	b = a * d - c + 5 / 6 / a;
	c = d / e % f - g + h + h;
	d = c - e % a - k;
	e = a + b + d / 3;
	f = 9 / 8 % 7 * 6 / 5 % 4 * 3 / 2 % 1 + 10;
	g = 10 - 12 / 4 - - - - - - - - - - - - - - - - - - - - 3;
	h = g / a * b / c * d % e;
	j = 12 - 12 / - 12 - - - 12 - - - - 12 - - - - - 12 - - - - - - 12;
	k = a * b * c * d * e * f / g % h - 10;
	l = a / b / c / d / e / f / g;
	m = (l * d / a) % (10 + b / (c % d) / k);
	return m;
 }
