//test return 12100111
// Author: Nick
int main () {
  int max =  2147483647;
  int min = -2147483648;

  int sum   = max + min;
  int diff1 = max - min;
  int diff2 = min - max;
  int prod  = max * min;
  int quot1 = max / min;
  int quot2 = min / max;
  int mod1  = max % min;
  int mod2  = min % max;

  return -sum    + 10 * (
         -diff1  + 10 * (
         diff2   + 10 * (
         2*prod  + 10 * (
         quot1   + 10 * (
         -quot2  + 10 * (
         -2*mod1 + 10 * (
         -mod2)))))));
}
