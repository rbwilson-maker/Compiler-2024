//test return -10229696

int main(){
    int a = 5 + 15 + 20 + 40*5 + 16 + 40;
    int b = 5 * 15 % 90 + 40 / 8 + 5;

    a = b * a + a % b;
    int c = (16 + a)*(6000+b) / -15;

    int x = 5;
    int y = 60;
    int z = 100;
    int k = z - y - x * 8 + (13-13) * 8 + 0;
    k = k * k;
    k = k + k;

    return c + k;
}