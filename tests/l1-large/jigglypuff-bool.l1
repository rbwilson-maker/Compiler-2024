//test error

/* Bool Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:27:20 AM 
 *
 * This test tests whether the compiler detects that the keyword 'bool'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int bool;

    x = 4;
    y = 3;
    bool = x + y;

    return bool;
}