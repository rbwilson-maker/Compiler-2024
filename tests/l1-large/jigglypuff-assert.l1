//test error

/* Assert Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:27:06 AM 
 *
 * This test tests whether the compiler detects that the keyword 'assert'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int assert;

    x = 4;
    y = 3;
    assert = x + y;

    return assert;
}