//test return 19
// tests parentheses

int main()
{
    int x = 41;
    x = (x + 6) * (x - 4);            // x = 1739
    x -= (((3 + 4 * 7) * 4) * 7 + 1); //x = 870;
    return (x / (3 * ((((87 / (4 + 5 * 5))))))) / (-21 / (-2 - - -5 % -3));
}
