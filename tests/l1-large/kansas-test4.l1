//test div-by-zero

// This is designed to test a for division by 0 exception.
int main()
{
  int a = 0;
  int b = 1;
  int c = 2;
  int d = 3;
  int e = 4;

  e += c * d + 0 * (b / a);
  return e;
}
