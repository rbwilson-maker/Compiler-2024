//test return 605

int main() {
  int x = 5;
  int y = 10;
  x = x + y;
  x = x + y;
  x += y;
  int z = y;
  z = x + y;
  x = z + x + y;
  x = x + x + x + x + x + x + y + z + y;
  int p = 32;
  z = 40;
  return x;
}
