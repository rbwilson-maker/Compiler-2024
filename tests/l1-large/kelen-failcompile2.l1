//test error

// Tests using a variable name ("z") that has not been declared
int main(){

    int x = 1;
    int y = 2;
    x = x + y;
    x = x + z; // Should not compile because z is not declared before use
    return x;

}