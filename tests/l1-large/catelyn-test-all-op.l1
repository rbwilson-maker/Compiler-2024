//test return -15
// this test intends on testing:
/* Simple comments */
// + - / * %
// = ++ -= *= /= %=^
// ?? -- ++ ?? Not sure about these

int main() {
    int a;
    int b;
    int c;
    int x;
    int y;
    int z;

    a = 0;
    b = 0;
    c = 0;
    x = 1;
    y = 2;
    z = 3;

    a = a + b;
    b = a + b;
    c = a + b;

    x = a - b;
    x = x - y;
    y = y - z;

    x = x/x;
    y = y/x;
    x = y/x;
    x = x/z;

    x = x*5 + 1;
    y = y*y*y + 3;
    z = z*z + 2*z + 1;

    a = a%x;
    b = b%y;
    c = c%z;

    x = 5;
    x += 1;
    y = 6;
    y -= 1;
    z *= -1;
    z /= -2;
    x %= 4;

    return a + b + c - x - y - z;
}
