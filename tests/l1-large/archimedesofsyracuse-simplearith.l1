//test return 1
// Test simple arithmatic using different valid asnop and lvalue
int main()
{
  int x;
  int y = 2;
  x = -y;
  (x) *= -1;
  ((x)) += 0;
  (((y))) /= 2;
  ((((x)))) -= 1;
  return (y * x);
}