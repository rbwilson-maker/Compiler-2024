//test error

/* Struct Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:26:48 AM 
 *
 * This test tests whether the compiler detects that the keyword 'struct'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int struct;

    x = 4;
    y = 3;
    struct = x + y;

    return struct;
}