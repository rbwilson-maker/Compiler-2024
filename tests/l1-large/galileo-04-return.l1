//test return 1
// cascading divs

int main() {
    int x;

    x = 1048576;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;
    x /= 2;

    return x;
}

