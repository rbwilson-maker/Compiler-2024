//test div-by-zero
// complex divide by zero
int main() {
    int x = 1;
    int y = 12;
    int z = 15;
    y = y * z;
    z = z - y*x;
    x = (x + z) % 123;
    x -= 81;
    x += 122;
    return y / x;
}
