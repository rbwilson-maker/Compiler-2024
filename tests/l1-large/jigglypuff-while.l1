//test error

/* While Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:26:56 AM 
 *
 * This test tests whether the compiler detects that the keyword 'while'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int while;

    x = 4;
    y = 3;
    while = x + y;

    return while;
}