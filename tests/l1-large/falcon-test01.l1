//test return 600

int main() {
    int x;
    int y;
    int z;
    int a;
    int b1;
    int b2;

    x = 4;
    x = 2;
    y = 10;
    z = x; // 2
    z = z * y; // 2 * 10 = 20
    b1 = z + y; // 20 + 10 = 30
    b1 = b1 * z; // 30 * 20 = 600; this will be the return value
    a = 1114;
    a = a * a;
    b2 = b1;
    return b1 + (a - a) + a - (z + a) + z;
}
