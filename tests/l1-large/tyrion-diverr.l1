//test div-by-zero

// Compilers f2017: Group Tyrion Lannister

int main() {
    int x = -1;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2;
    x *= 2; 
    x *= 2;
    x /= -1;
    return x;
}
