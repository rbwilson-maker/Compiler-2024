//test return 0
// x and y have a pokemon battle, then x decides to run away
int main()
{
    int x;
    int y = 5;
    x = 1;
    x += y;
    y *= x;
    x /= y;
    y -= x;
    x += 3;
    x %= x;
    return x;
}
