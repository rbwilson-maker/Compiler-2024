//test return 15

int main() {
    int x;
    int y;
    int z;
    
    x = 1;      // 1
    y = x + 1;  // 2
    z = y + 1;  // 3
    
    x = z + 1;  // 4
    y = x + 1;  // 5
    z = y + 1;  // 6
    
    return x + y + z;
}