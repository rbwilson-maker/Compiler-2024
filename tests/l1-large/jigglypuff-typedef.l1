//test error

/* Typedef Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:26:50 AM 
 *
 * This test tests whether the compiler detects that the keyword 'typedef'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int typedef;

    x = 4;
    y = 3;
    typedef = x + y;

    return typedef;
}