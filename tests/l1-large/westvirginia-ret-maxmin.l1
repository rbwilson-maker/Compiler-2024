//test return 0
// test arithmetic on INTMAX and INTMIN

int main() {
    int intmax = 0x7fffffff;
    int intmin = 0x80000000;
    int x;

    x = intmax * 2 + (intmin / intmax) + (intmax * intmin) + 3 * (intmin - intmax);
    return 2 * x;
}
