//test return 821

int main() {

    int p0 = 2;
    int p1 = 3;
    int p2 = 5;
    int p3 = 7;
    int p4 = 11;
    int compilers = p0 * p1 * p2 * p0 * p1 * p2 - 500 + p4; 
    int os = p0 * p1 * p2 * p3 + p0 * p2 * p0 * p2 * p0;
    
    return compilers + os;
}