//test div-by-zero
// division error due to overflow
int main()
{
  int TMIN;
  int overflow;

  TMIN = -2147483648;
  int TMAX = TMIN-1;
  overflow = 1 / (TMIN -1 + TMAX + 2);
  return overflow;
}

  
