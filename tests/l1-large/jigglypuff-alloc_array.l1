//test error

/* Alloc_array Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:27:16 AM 
 *
 * This test tests whether the compiler detects that the keyword 'alloc_array'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int alloc_array;

    x = 4;
    y = 3;
    alloc_array = x + y;

    return alloc_array;
}