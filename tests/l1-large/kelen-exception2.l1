//test div-by-zero

// Tests modulo with zero
int main() {
    int x = -3;
    int y = -2;
    x *= y;         // x = 6
    y += 2;         // y = 0
    x %= y;         // raise an exception
    return x;
}