//test error

/* String Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:27:26 AM 
 *
 * This test tests whether the compiler detects that the keyword 'string'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int string;

    x = 4;
    y = 3;
    string = x + y;

    return string;
}