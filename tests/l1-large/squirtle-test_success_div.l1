//test return 1
//test to make sure that division works

int main() {
  int x;
  int y; 
  int z;
  
  x = 1;
  y = 3;
  
  x = x + x; // x = 2
  y = y * y; //  y = 9

  z = x/y + (x%y); // z = 2
  z = z + (z / (x +  x/x)); // z = 2;

  return (z * z * z * z) / y;
}
