//test return 99992
// normal program 1

int main(){
    int x = 1;
    int y;
    y = 3;
    int z = x + y;
    z = z - 1;
    z = -5;
    z = x - 5;
    int t = z + 1000;
    t = t - 100000;
    t = z + 100000;
    return t + z - x / y;
}