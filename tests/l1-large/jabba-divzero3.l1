//test div-by-zero

int main() {
    int x = 15;
    int y = 13;
    int z;
    z = y % x;
    x = y;
    y = z;
    z = y % x;
    x = y;
    y = z;
    z = y % x;
    x = y;
    y = z;
    z = y % x;
    x = y;
    y = z;
    z = y % x;
    x = y;
    y = z;
    z = y % x;
    x = y;
    y = z;
    z = y % x;
    x = y;
    y = z;
    return y;
}