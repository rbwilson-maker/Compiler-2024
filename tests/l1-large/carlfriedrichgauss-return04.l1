//test return -2
// test operator precedence
int main() {
    int x = 1;
    int y = 2;
    int z = 3;
    int res1 = x + y * z; // 7
    int res2 = (x + y) * z; // 9
    int diff = res1 - res2;
    return diff;
}
