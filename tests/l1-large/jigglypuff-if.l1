//test error

/* If Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:26:52 AM 
 *
 * This test tests whether the compiler detects that the keyword 'if'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int if;

    x = 4;
    y = 3;
    if = x + y;

    return if;
}