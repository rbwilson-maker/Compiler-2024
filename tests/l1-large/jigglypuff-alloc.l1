//test error

/* Alloc Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:27:14 AM 
 *
 * This test tests whether the compiler detects that the keyword 'alloc'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int alloc;

    x = 4;
    y = 3;
    alloc = x + y;

    return alloc;
}