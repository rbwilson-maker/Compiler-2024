//test return 3
//Tests re-assignment of variables, alongside with some basic arithmetic.

int main()
{
  int x = 1;
  int y = 2;
  y = y - x; // y = 1
  x = 2*x + 2*y; // x = 4
  x = 3*y; //x = 3
  return x;
}
