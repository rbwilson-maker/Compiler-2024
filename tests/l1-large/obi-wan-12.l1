//test return 5

// this tests all of the assignment operations for L1
int main()
{
    int x;

    x = 3;
    x += 10; // 13
    x -= 5;  // 8
    x *= 3;  // 24
    x /= 2; // 12
    x %= 7; // 5

    return x;
}

