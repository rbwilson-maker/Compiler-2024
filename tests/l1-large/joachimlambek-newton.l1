//test return 46340

int main() {
    int x = 1;
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    x = x / 2 + 0x7fffffff / (2 * x);
    return x;
}
