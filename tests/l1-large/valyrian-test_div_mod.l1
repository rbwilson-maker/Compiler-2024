//test return 31

int main() {
  // 1
  int x = 5 / 4;

  // 3
  int y = 10 / 3;

  // 2
  int z = 200 % 3;

  // 5
  int w = 5 % 100;

  // 5
  int q = (5 / 4) * 4 + (5 % 4);

  // 15
  int r = (15 / 7) * 7 + (15 % 7);

  return x + y + z + w + q + r;
}
