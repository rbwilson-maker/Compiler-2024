//test return 4
// test various arithmetic operations

int main() { 
    int x = 60;
    int y = 4;
    int z = x / y;
    z += 10;
    x -= 5;
    z -= x;
    z *= 0;
    z += y;
    z %= x;
    return z; 
}
