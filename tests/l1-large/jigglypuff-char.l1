//test error

/* Char Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:27:24 AM 
 *
 * This test tests whether the compiler detects that the keyword 'char'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int char;

    x = 4;
    y = 3;
    char = x + y;

    return char;
}