//test error

int main(){
    int a = 5;
    int b = 10;
    int c = 15;

    a += b;
    a += c;
    c += a;
    c = c + a;
    b = 20;

    a = a/b;

    b = 20

    c += c + 20;
    c += b + 10;
    c += a * 10;
    c -= 90;
    a = c + a;


    return 0;
}