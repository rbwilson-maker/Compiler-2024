//test error

/* For Keyword Test
 *
 * Brandon Perez (bmperez)
 * Xiaofan Li (xli2)
 * Sunday, September 13, 2015 at 09:26:58 AM 
 *
 * This test tests whether the compiler detects that the keyword 'for'
 * is being used as an identifier.
 */

int main()
{
    int x;
    int y;
    int for;

    x = 4;
    y = 3;
    for = x + y;

    return for;
}