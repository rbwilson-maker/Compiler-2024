//test return 100

void test(int n) {
  int[] f;
  for (int i = 0; i < 5; i++) {
    f = alloc_array(int, n+1);
  }
  return;
}

int main () {
  int *n = alloc(int);
  *n = 100;
  test(*n);
  return 100;
}