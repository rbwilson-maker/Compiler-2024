//test memerror
			 // can you catch this out of bounds error? 
             int main() {	  int i = ((1 << 30)-1)*2 + 1;

  bool[] a = alloc_array(bool, 1);   return a[i] ? 0 : 1;										}																																			
