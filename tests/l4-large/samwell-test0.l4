//test return 0

int main() {
  int* p = alloc(int);
  int* q = p; 
  *p = 10;
  int[][] arr = alloc_array(int[], *q);
  arr[0] = alloc_array(int, *q);
  return arr[0][0];
  }

// Read the handout and look at the files in ../../tests/l1-basic
// for more information.
