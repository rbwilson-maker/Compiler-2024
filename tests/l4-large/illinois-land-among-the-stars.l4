//test return 1776
// Shoot for the moon. Even if you miss, you'll land among the stars~

int************************************************** apollo_11(int val) {
  /*****/ int************************************************** p;
  p = alloc(int*************************************************);
  *p = alloc(int************************************************);
  **p = alloc(int***********************************************);
  ***p = alloc(int**********************************************);
  ****p = alloc(int*********************************************);
  *****p = alloc(int********************************************);
  ******p = alloc(int*******************************************);
  *******p = alloc(int******************************************);
  ********p = alloc(int*****************************************);
  *********p = alloc(int****************************************);
  **********p = alloc(int***************************************);
  ***********p = alloc(int**************************************);
  ************p = alloc(int*************************************);
  *************p = alloc(int************************************);
  **************p = alloc(int***********************************);
  ***************p = alloc(int**********************************);
  ****************p = alloc(int*********************************);
  *****************p = alloc(int********************************);
  ******************p = alloc(int*******************************);
  *******************p = alloc(int******************************);
  ********************p = alloc(int*****************************);
  *********************p = alloc(int****************************);
  **********************p = alloc(int***************************);
  ***********************p = alloc(int**************************);
  ************************p = alloc(int*************************);
  *************************p = alloc(int************************);
  **************************p = alloc(int***********************);
  ***************************p = alloc(int**********************);
  ****************************p = alloc(int*********************);
  *****************************p = alloc(int********************);
  ******************************p = alloc(int*******************);
  *******************************p = alloc(int******************);
  ********************************p = alloc(int*****************);
  *********************************p = alloc(int****************);
  **********************************p = alloc(int***************);
  ***********************************p = alloc(int**************);
  ************************************p = alloc(int*************);
  *************************************p = alloc(int************);
  **************************************p = alloc(int***********);
  ***************************************p = alloc(int**********);
  ****************************************p = alloc(int*********);
  *****************************************p = alloc(int********);
  ******************************************p = alloc(int*******);
  *******************************************p = alloc(int******);
  ********************************************p = alloc(int*****);
  *********************************************p = alloc(int****);
  **********************************************p = alloc(int***);
  ***********************************************p = alloc(int**);
  ************************************************p = alloc(int*);
  *************************************************p = alloc(int);
  **************************************************p = val; /***/
  return p; /****************************************************/
}

int main() {
  return
         *               *                    *            *       * 
 *           *     *        /***************/       *          *        
      *              /*******               *******/      *          *    
             *    /***                             ***/         *   
  *      *     /***                                   ***/            *  
             /**                                         **/      *   
      *    /**                                             **/        *  
         /**                                                 **/   
  *     /*                                                     */   *
       /*                                                       */ 
  *   /*                                                         */  *
      /*                                                         */
*    /*                                                           */   * 
     /*                                                           */
    /*                                                             */
    /*                                                             */ *  
 *  /*                                                             */
    /*                                                             */
     /*                                                           */   * 
  *  /*                                                           */ *  
      /*                                                         */
 *    /*                                                         */  *  
       /*                                                       */
  *     /*                                                     */     * 
         /**                                                 **/   *   
           /**                                             **/
    *        /**                                         **/     *   *  
               /***                                   ***/
   *      *       /***                             ***/   *    *   
                     /*******               *******/              *   
  *     *        *          /***************/            *           *      
                                             apollo_11 (1969-7-20)
  -166;
}
