//test return 1

struct A {
    int x;
    int y;
};

int main () {
    struct A *a = alloc(struct A);
    a->x = 4;
    a->y = 3;
    for (int i = 0; i < (a->y); i++) {
        a->x--;
    }
    return (a->x);
}