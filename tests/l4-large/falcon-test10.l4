//test memerror
// What came first, the chicken or the egg?

int main() {
    int[] A = alloc_array(int, 9);

    for(int i = 0; i < 10; i++) {
        A[i] = 1000000000 / (9 - i);
    }

    return 0;

}