//test return 42
// 1. Structs can be declared implicitly.
// 2. Default value of t[] is 0.

bool olderthanthetrees(struct westvirginia[] youngerthan, 
                       struct westvirginia[] the_mountains) {
    return (youngerthan == the_mountains);
}

int main() {
    struct westvirginia[][] blueridgemountains = 
        alloc_array(struct westvirginia[], 20);
    struct shenandoahriver*[] lifeisoldthere = alloc_array(struct 
       shenandoahriver*, 50);
    return olderthanthetrees(blueridgemountains[0], blueridgemountains[19])
      ? 42 : 41;
}
