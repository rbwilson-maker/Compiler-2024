//test return 15411
// a sky full of stars

int main() {
  int****************************** star;

  star = alloc(int*****************************);
  *star = alloc(int****************************);
  **star = alloc(int***************************);
  ***star = alloc(int**************************);
  ****star = alloc(int*************************);
  *****star = alloc(int************************);
  ******star = alloc(int***********************);
  *******star = alloc(int**********************);
  ********star = alloc(int*********************);
  *********star = alloc(int********************);
  **********star = alloc(int*******************);
  ***********star = alloc(int******************);
  ************star = alloc(int*****************);
  *************star = alloc(int****************);
  **************star = alloc(int***************);
  ***************star = alloc(int**************);
  ****************star = alloc(int*************);
  *****************star = alloc(int************);
  ******************star = alloc(int***********);
  *******************star = alloc(int**********);
  ********************star = alloc(int*********);
  *********************star = alloc(int********);
  **********************star = alloc(int*******);
  ***********************star = alloc(int******);
  ************************star = alloc(int*****);
  *************************star = alloc(int****);
  **************************star = alloc(int***);
  ***************************star = alloc(int**);
  ****************************star = alloc(int*);
  *****************************star = alloc(int);

  ******************************star = 15411;
  return ******************************star;
}
