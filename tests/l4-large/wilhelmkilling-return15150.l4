//test return 15150

int main () {
    int[] meow = alloc_array(int, 5);
    meow[0] = 1;
    meow[1] = 5;
    meow[2] = 1;
    meow[3] = 5;
    meow[4] = 0;

    return (meow[0] * 10000) + (meow[1] * 1000) + (meow[2] * 100) + (meow[3] * 10) + meow[4];
}