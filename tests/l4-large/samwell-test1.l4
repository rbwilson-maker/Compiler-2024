//test error

typedef int foo;
foo func();

struct foo;

struct foo{
  int x;
  int y;
}

struct foo foo () {
  return 0;
}

int main(){
  alloc (int);
  arr = alloc_array(bool, 15);
  bool i = arr[0] ^ arr[14];
  A[f(x)] += 3;
  foo* i;
  foo2 * foo3;
  return 0;
}
// Read the handout and look at the files in ../../tests/l1-basic
// for more information.
