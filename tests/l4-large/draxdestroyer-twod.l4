//test typecheck
int main() {

    int [][] twod = alloc_array(int [], 0);
    for (int i = 0; i < 0 ; i++) {
        twod[i] = alloc_array(int, 0);
    }

    int sum =0 ;
    for (int i = 0 ; i < 0 ; i++) {
    for (int j = 0 ; i < 0 ; i++) {
        sum += twod[i][j];
    }
    }
    return sum;
}
