//test abort
// abort with assert

int main() {
    int size = 10000;
    int[] arr = alloc_array(int, size);
    for (int i = 0; i < size; i += 1) {
        arr[i] = i;
    }
    assert(arr[0] != 0);
    return 0;
}
