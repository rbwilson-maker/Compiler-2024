//test return 100

typedef struct lol lolomg;

struct lol {
  lolomg * lolomg;
  struct lolomg * lol;
  struct lol * lol2;
};

struct lolomg {
  lolomg * wow;
  int value;
};

int main() {
  lolomg * wow = alloc(struct lol);
  wow->lol = alloc(struct lolomg);
  wow->lol->value = 100;
  return wow->lol->value;
}
