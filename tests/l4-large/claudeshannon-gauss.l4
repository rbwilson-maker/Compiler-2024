//test return 5050

int main() {
    int[] A = alloc_array(int, 100);

    int* x = alloc(int);

    for (int i = 0; i < 100; i++) {
        A[i] = i + 1;
    }

    for(int i = 0; i < 100; i++) {
        *x += A[i];
    }

    return *x;
}