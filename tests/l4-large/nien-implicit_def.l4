//test typecheck

int main() {
    int len = 12;
    int sum = 0;
    struct hello * [] a = alloc_array(struct hello *, len);
    for (int i = 0; i < len; i++) {
        sum += i;
    }
    return sum;
}
