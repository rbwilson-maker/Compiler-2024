//test error

typdef int* array_elem;

int main () {

    int size = 10;

    int[] arr = alloc_array(int, size);
    array_elem[] arr = alloc_array(array_elem, size);

    for (int i = 0; i < size; i++) {
    	int k = arr[3] * 4;
    }

    return k;

}