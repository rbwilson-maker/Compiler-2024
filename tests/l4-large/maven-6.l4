//test error
// No argc or argv allowed in main

void test(int n) {
  int[] f;
  for (int i = 0; i < 5; ++i) {
    f = alloc_array(int, n+1);
  }
  return;
}

int main (int argc, char **[] argv) {
  int *n = alloc(int)
  *n = 100
  test(n);
  return 100;
}