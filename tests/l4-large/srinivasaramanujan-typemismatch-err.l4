//test error

int array_iter(int[] arr, int n) {
    int i = 0;
    int sum = 0;
    for (i = 0; i < n; i++) {
        sum += arr[i];
    }

    return arr[0];
}

int main() {
    int* q;
    array_iter(q,0);
    return 0;
}
