//test return 5

void swap(int[] A, int i, int j) {
     int x = A[i];
     A[i] = A[j];
     A[j] = x;
}

int main() {
    int[] A = alloc_array(int, 3);
    A[0] = 1;
    A[1] = 5;
    A[2] = 3;
    swap(A, 1, 2);
    swap(A, 0, 2);
    return A[0];
}
