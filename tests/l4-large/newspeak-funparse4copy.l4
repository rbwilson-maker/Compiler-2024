//test return 0
// test the non-context free parsing

// typedef bool ty;

int main() {
  int ty = 3; // typedef used as variable name
  int ret = 4;
  int x = 0;
  for(ty * ret; x < 3; x++) {
    ty * ret;
  }
  return 0;
}