//test div-by-zero
// Check that arrays are correctly calloc'd and not malloc'd

int main() {
    int all_zeros = 0;
    int[] x = alloc_array(int, 10000);
    for(int i = 0; i < 10000; i++) {
        all_zeros += x[i];
    }

    return 1 / all_zeros;
}
