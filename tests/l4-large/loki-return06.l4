//test return 10

int main() {
  int len = 10;
  int*[] arr = alloc_array(int*, len);
  int count = 0;
  for (int i = 0; i < len; i++) {
    if (arr[i] == NULL) {
      count++;
    }
  }
  return count;
}
