//test return 14

struct node {
  int val;
  struct node *next;
};
typedef struct node node_t;

int main()
{
  node_t *start = alloc(node_t);
  start->val = 2;
  start->val *= 7;
  return start->val;
}
