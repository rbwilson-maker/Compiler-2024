//test return 55
//array sum

int sum(int[] a) {
    int sum = 0;
    for(int i=0; i < 10; i++) {
        sum += a[i];
    }
    return sum;
}

int main() {
    int[] a = alloc_array(int, 10);
    for(int i=0; i<10; i++) {
        a[i] = i+1;
    }
    return sum(a);
}