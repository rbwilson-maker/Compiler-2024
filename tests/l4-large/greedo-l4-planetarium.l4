//test typecheck

int main()
{
  int********* p;
  p = alloc(int********);
  *p = alloc(int*******);
  **p = alloc(int******);
  ***p = alloc(int*****);
  ****p = alloc(int****);
  *****p = alloc(int***);
  ******p = alloc(int**);
  *******p = alloc(int*);
  ********p = alloc(int); 
  return *********
  pluto(*********p,********p,*******p,******p,*****p,****p,***p,**p,*p);
}
