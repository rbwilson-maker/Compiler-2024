//test memerror

int main () {
    int[][] A = alloc_array(int[], 10);
    A[0] = alloc_array(int, 0);
    A[1] = alloc_array(int, 0);
    if (A[0] == A[2]) {
       return 1;
    }
    if (A[0] == A[1]) {
       return 0;
    }
    return A[11][0];
}