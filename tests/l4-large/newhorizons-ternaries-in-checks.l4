//test return 20

int main() {
    int[] arr = alloc_array(int, 5);
    for (int i = 0; i < 5; i++) {
        arr[i] = 0;
    }
    int x = 20;
    while((x>0 ? 1 : -1) > 0){
        arr[2]++;
        x--;
    }
    return arr[2];
}