//test return 50

int f(int x);

int main(){
  int[] array = alloc_array(int, 10);
  for(int i=0; i < 10; (((((((((((((((array[f(i)])))))))))))))))++)
    i++;
  return 50;
}

/* This code has a lot of effects... It's important to actually run this :) */
int f(int x){
  // Write to memory
  int* pointer = alloc(int);
  *pointer = x;

  // Print stuff
  print_int(x);

  return x - 1;
}
