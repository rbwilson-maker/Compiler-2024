//test error

struct s1 { int f1; int f2; };

struct s2 {
    int i; 
    struct s1 t; 
    int* p; 
};

int main() {
    struct s2* a = alloc(struct s2); 
    struct s1* b = alloc(struct s1); 
    a->t = *s1; 
    return s->t.f1; 
}
