//test return 100

int main(){
  int[] a = alloc_array(int, 10);
  int[] b = alloc_array(int, 10);
  int i = 0;
  for(; i < 10; i++){
    a[i] = i;
    b[i] = 10 - i;
  }
  int sum = 0;
  for(i = 0; i < 10; i++){
    sum += (a[i] + b[i]);
  }
  return sum;
}
