//test compile
struct nouse;
struct nouse 
{
    int i;
    int* j;
    bool* s;
};

int main() { 
    int i=2<<10;
    int j=79 % 2;
    for(int s=ftoi(fadd(i,j)); true; i=i>>1)
    {
        j-=j % i;
    }
    return j+i % 1;
}