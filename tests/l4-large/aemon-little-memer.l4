//test return 2

int main() {
    int[] A = alloc_array(int, 4);
    int[] B = alloc_array(int, 4);
    for (int i = 0; i < 4; i++) {
        A[B[i]] = B[A[i]] + 1;
        B[i]++;
    }

    return A[0];
}
