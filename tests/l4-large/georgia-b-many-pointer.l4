//test return 63

int main() {
    int *****p = alloc(int ****);
    *p = alloc(int ***);
    **p = alloc(int **);
    ***p = alloc(int *);
    ****p = alloc(int);
    *****p = 63;

    return *****p;
}
