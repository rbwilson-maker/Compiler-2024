//test return 0
 
typedef int a;		 struct strucc {
  a *[][]* b;   a []**[] c;
};																																	typedef struct strucc struck;  int foo(struck * strucc) {								  (*strucc).b;   strucc->c;

  int c = 1; 
  a * b;
  c * c;																														  b = alloc(int);

  *b**b;																									  c**b;   *b*c;
																													  return * b;																												}
 int main() {   return 0;																		}
																								