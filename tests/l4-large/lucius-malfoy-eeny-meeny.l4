//test typecheck

int main() {
    struct emmm *first = NULL;
    struct gethim *second = NULL;

    if (he_hollers(second->tiger)) {
        let_go(second->tiger);
    } else {
        eeny meeny = alloc_array(int*, 1);
        miny moe = alloc(int[]);
    }

    return 3;
}
