//test return 0

typedef int boo;
typedef boo bee;
typedef bool bii;

struct point {
  int x;
  int y;
};

typedef struct point buu;

bii fa() {
  return false;
}

bii isfour(boo x) {
  bool truth = true;
  bii cmp_result = 4 == x;
  if (cmp_result == truth)
    return truth;
  else
    return !truth;
}

int main() {
  bee * a = NULL;
  if (isfour(3))
    return 1;
  return 0;
}
