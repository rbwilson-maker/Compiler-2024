//test error 
struct fee;
typedef struct fee fee;
typedef struct foo foo;
struct foo{
    fee fee;
    bool * b;
};
struct fee {
    struct foo foo;
    int fee;
};
int main() {
    fee * x = alloc(fee);
    foo* y = alloc(foo);
    return 0;
}
    
