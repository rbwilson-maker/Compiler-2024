//test return 14

int main() {
  int[][] a = alloc_array(int[], 10);
  for (int i=0; i<10; i++) {
    a[i] = alloc_array(int, 10);
  }
  for (int i=0; i<10; i++) {
    for (int j=0; j<10; j++) {
      a[i][j] = i+j;
    }
  }
  return a[5][9];
}
