//test return 934907996

//a function that finds the product of x and y without using *
int mul(int x, int y) {
  int[] a = alloc_array(int, x);
  for (int i = 0; i < x; i++) {
    a[i] = y;
  }
  int total = 0;
  for (int i = 0; i < x; i++) {
    total += a[i];
  }
  return total;
}

int main() {
  return mul(mul(mul(419,1), mul(209,1)), mul(mul(34,1),mul(314,1)));
}
