//test error
/* non-convertible type */

int main () {
  int ***x = alloc(int ***);
  int ***y = alloc(int ***);
  int ***z = alloc(int ***);
  int ***a = alloc(int ***);
  int ***b = alloc(int ***);
  int ***c = alloc(int ***);
  int ***d = alloc(int ***);
  int ***e = alloc(int ***);
  int ***f = alloc(int ***);
  int ***g = alloc(int ***);
  int ***h = alloc(int ***);
  int ***z = alloc(int ***);
  int ***s = alloc(int ***);
  int** y;
  *x = y;
  return 0;
}
