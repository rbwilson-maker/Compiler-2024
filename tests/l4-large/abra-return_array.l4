//test return 5050

int main() {
	int[] a = alloc_array(int, 100);
	for (int i = 1; i <= 100; i++) {
		a[i - 1] = i;
	}

	int accum = 0;
	for (int i = 0; i < 100; i++) {
		accum += a[i];
	}
	return accum;
}