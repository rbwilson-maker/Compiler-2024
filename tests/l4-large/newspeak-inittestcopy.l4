//test return 0
// test initialization by alloc_array

int main() {
  int N = 100000;
  int[] x = alloc_array(int, N);
  for(int i = 0; i < N; i++) {
    if(x[i] != 0) {
      return 1;
    }
  }
  return 0;
}