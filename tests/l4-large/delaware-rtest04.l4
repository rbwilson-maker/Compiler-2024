//test return 20500

int main() {
   int[] foo;
   foo = alloc_array(int, 200);
   for (int i = 0; i < 200; i++)
      foo[i] = i+3;
   int z = 0;
   for (int i = 0; i < 200; i++) z += foo[i];
   return z;
}
