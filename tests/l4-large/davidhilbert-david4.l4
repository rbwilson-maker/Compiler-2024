//test return 0

int get_ith_elem(int[] arr, int i) {
    return arr[i];
}

int main() {
    int n = 40000;
    int[] arr = alloc_array(int, n);
    for (int i = 0; i < n; i++) {
        get_ith_elem(arr, i);
    }
    return 0;
}