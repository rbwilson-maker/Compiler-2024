//test return 3

int main() {
	int[]***** a = alloc(int[] ****);
	*a = alloc(int[] ***);
	**a = alloc(int[] **);
	***a = alloc(int[] *);
	****a = alloc(int[]);
	*****a = alloc_array(int, 1);
	(*****a)[0] = 3;
	return (*****a)[0];
}
