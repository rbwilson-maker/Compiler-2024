//test typecheck
//test infloop
//passing struct on stack a lot!

struct asdf
{
  int a;
  int b;
  int c;
  int d;
  int e;
  int f;
  int g;
  int h;
  int i;
  int j;
  int k;
  int l;
  int m;
  int n;
  int o;
  int p;
  int q;
  int r;
  int s;
  int t;
  int u;
  int v;
  int w;
  int x;
  int y;
  int z;
};

struct fdsa
{
  int l;
  int m;
  int n;
  int o;
  int p;
  int q;
  int r;
  int s;
};

int fn(int i, struct asdf* coco) {
  if (i == 0)
    return coco->a;
    struct asdf* girafff = alloc(struct asdf);
    coco->a += 20;
    coco->a += 20;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    girafff->z += 30;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    coco->a += 20;
    coco->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    coco->z += 30;
    coco->u -= 60;
    girafff->a += 20;
    girafff->z += 30;
    coco->m -= coco->z + girafff->u;
    girafff->q = coco->d + coco->z + coco-> m;
    girafff->m -= coco->z + girafff->u;
    coco->q = coco->d + coco->z + coco-> m;
    girafff->z += 30;
    girafff->a += 20;
    girafff->z += 30;
    girafff->u -= 60;
    coco->m -= coco->z + coco->u;
    coco->q = coco->d + coco->z + coco-> m;
    int Coco = coco->q + (coco->a % coco->m);
    return coco->q + Coco + fn(i-1, coco) / Coco + fn(i-1,girafff);
}

int main()
{
    struct asdf* mankey = alloc(struct asdf);
    mankey->m = 1000;
    return fn(36488888, mankey);
}

