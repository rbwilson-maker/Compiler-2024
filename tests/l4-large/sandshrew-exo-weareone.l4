//test error

struct main {
  int main;
};

typedef int foo;
struct foo {
  foo foo;
};

int main() {
  struct main* main = alloc(struct main);
  main->main = 1;
  
  struct main* maine = alloc(struct main);
  *maine = *main;

  return 0;
}
