//test return 0

int main() {
 int[] A = alloc_array(int, 42);
 for (int i = 0; i < 42; i++)
 //@loop_invariant \forall {A} (\element >= 0);
 //@loop_invariant \exists {A} (\element == 0);
 {
   A[i] = (i);
 }
 return 0;
}
